<Window x:Class="ExhibitionManagementSystem.DeskTop.Views.Auth.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="ExpoManager — تسجيل الدخول" 
        Width="1000" Height="650" 
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft"
        Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <!-- Left side: Form -->
            <ColumnDefinition Width="*"/>
            <!-- Right side: Brand Panel -->
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left side: Form -->
        <Border Grid.Column="0" Background="White" Padding="60">
            <StackPanel VerticalAlignment="Center" MaxWidth="380">

                <!-- Logo/Title -->
                <TextBlock Text="ExpoManager" Style="{StaticResource H2Style}" Margin="0,0,0,8"/>
                <TextBlock Text="مرحباً بك، سجّل دخولك للمتابعة"
                           Style="{StaticResource BodySecondaryStyle}"
                           Margin="0,0,0,32"/>

                <!-- Email Input -->
                <TextBlock Text="البريد الإلكتروني" Style="{StaticResource LabelStyle}" Margin="0,0,0,8"/>
             
                <TextBox Style="{StaticResource StandardTextBoxStyle}"
                         Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,0,16"/>

                <!-- Password Input -->
                <TextBlock Text="كلمة المرور" Style="{StaticResource LabelStyle}" Margin="0,0,0,8"/>
                <PasswordBox x:Name="PasswordField"
                             Style="{StaticResource StandardPasswordBoxStyle}"
                             PasswordChanged="PasswordField_PasswordChanged"
                             Margin="0,0,0,8"/>

                <!-- Error Message -->
                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="{StaticResource DangerBrush}"
                           FontSize="13"
                           Visibility="{Binding HasError, Converter={StaticResource BoolToVisibility}}"
                           Margin="0,0,0,16"/>

                <!-- Loading indicator -->
                <ProgressBar IsIndeterminate="True" Height="3"
                             Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}"
                             Foreground="{StaticResource PrimaryBrush}"
                             Margin="0,0,0,16"/>

                <!-- Login Button -->
                <Button Content="تسجيل الدخول"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding LoginCommand}"
                        HorizontalAlignment="Stretch"
                        Margin="0,0,0,16"/>

                <!-- Forgot Password -->
                <Button Content="نسيت كلمة المرور؟"
                        Style="{StaticResource LinkButtonStyle}"
                        Command="{Binding ForgotPasswordCommand}"
                        HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Right side: Brand Panel -->
        <Border Grid.Column="1" Background="{StaticResource PrimaryGradientBrush}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="🏛️" FontSize="72" HorizontalAlignment="Center" Margin="0,0,0,24"/>
                <TextBlock Text="ExpoManager"
                           Foreground="White" FontSize="36" FontWeight="Bold"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="نظام إدارة المعارض المتكامل"
                           Foreground="White" FontSize="16" Opacity="0.85"
                           HorizontalAlignment="Center" Margin="0,12,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
