<UserControl x:Class="ExhibitionManagementSystem.DeskTop.Controls.Forms.SponsorshipPackageFormControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="450"
             FlowDirection="RightToLeft">
    <Border Background="White" CornerRadius="12" Padding="24">
        <StackPanel>
            <TextBlock Text="{Binding Title}" Style="{StaticResource H2Style}" Margin="0,0,0,16"/>

            <!-- Package Name -->
            <TextBlock Text="اسم الباقة *" Style="{StaticResource LabelStyle}" Margin="0,0,0,4"/>
            <TextBox Style="{StaticResource StandardTextBoxStyle}" 
                     Text="{Binding PackageName, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,0,0,12"/>

            <!-- Level & Max Sponsors -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="مستوى الرعاية" Style="{StaticResource LabelStyle}" Margin="0,0,0,4"/>
                    <ComboBox Style="{StaticResource StandardComboBoxStyle}" 
                              ItemsSource="{Binding Levels}" 
                              SelectedItem="{Binding Level}"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="الحد الأقصى للرعاة" Style="{StaticResource LabelStyle}" Margin="0,0,0,4"/>
                    <TextBox Style="{StaticResource StandardTextBoxStyle}" Text="{Binding MaxSponsorsCount, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
            </Grid>

            <!-- Price & Currency -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="سعر الباقة *" Style="{StaticResource LabelStyle}" Margin="0,0,0,4"/>
                    <TextBox Style="{StaticResource StandardTextBoxStyle}" Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="العملة" Style="{StaticResource LabelStyle}" Margin="0,0,0,4"/>
                    <ComboBox Style="{StaticResource StandardComboBoxStyle}" 
                              ItemsSource="{Binding Currencies}" 
                              SelectedItem="{Binding CurrencyCode}"/>
                </StackPanel>
            </Grid>

            <!-- Entitlements Description -->
            <TextBlock Text="المزايا والامتيازات المشمولة" Style="{StaticResource LabelStyle}" Margin="0,0,0,4"/>
            <TextBox Style="{StaticResource StandardTextBoxStyle}" 
                     Text="{Binding EntitlementsDescription, UpdateSourceTrigger=PropertyChanged}" 
                     Height="100" TextWrapping="Wrap" AcceptsReturn="True"
                     Margin="0,0,0,16"/>

            <!-- Actions -->
            <Button Content="حفظ باقة الرعاية" 
                    Style="{StaticResource PrimaryButtonStyle}" 
                    Command="{Binding SaveCommand}" 
                    Height="40"/>
        </StackPanel>
    </Border>
</UserControl>
