<Window x:Class="ExhibitionManagementSystem.DeskTop.Controls.Dialogs.FormDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding Title, RelativeSource={RelativeSource Self}}" 
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{StaticResource BgMainBrush}"
        BorderBrush="{StaticResource BorderMainBrush}"
        BorderThickness="1"
        MinWidth="500"
        MinHeight="300"
        FlowDirection="RightToLeft">
    <Border Padding="24" Background="{StaticResource BgCardBrush}" CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock x:Name="TitleText" Grid.Row="0" 
                       Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" 
                       Style="{StaticResource H2Style}" 
                       Margin="0,0,0,20"/>

            <!-- Form Content -->
            <ContentControl x:Name="DialogContent" Grid.Row="1"/>
        </Grid>
    </Border>
</Window>
