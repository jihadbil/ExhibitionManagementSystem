<UserControl x:Class="ExhibitionManagementSystem.DeskTop.Controls.Feedback.LoadingOverlayControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             x:Name="Root"
             d:DesignHeight="300" d:DesignWidth="500"
             Visibility="{Binding IsLoading, ElementName=Root, Converter={StaticResource BoolToVisibility}}">
    <Border Background="#80FFFFFF" CornerRadius="16">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <ProgressBar IsIndeterminate="True" Width="200" Height="4"
                         Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,12"/>
            <TextBlock Text="{Binding Message, ElementName=Root}"
                       Style="{StaticResource BodySecondaryStyle}"
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Border>
</UserControl>
